


<div class="col-md-1">
  <table class="table">
    <thead>
      <tr>
        <th scope="col"><%= image_tag(@product.main_image, :size => "380x350") %></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th scope="row"><%= @product.name %></th>
      </tr>
      <tr>
        <th scope="row"><%= @product.price %> ден</th>
      </tr>
    </tbody>
  </table>
</div>